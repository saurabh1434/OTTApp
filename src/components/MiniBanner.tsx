import React from "react";

const MiniBanner: React.FC = () => (
  <div style={{ backgroundColor: "#e2e2e2", padding: 20, textAlign: "center" }}>
    <p>🔥 Hot picks this week – click a thumbnail to start watching!</p>
  </div>
);

export default MiniBanner;
